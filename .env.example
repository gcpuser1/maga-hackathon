# Copy this file to .env and add the key. Git ignores .env. Never commit a real key.
# Pydantic AI's Google model reads GOOGLE_API_KEY (GEMINI_API_KEY also works).
GOOGLE_API_KEY=

# Optional. With a Logfire write token, each Gemini call is traced. Without it, nothing is sent.
LOGFIRE_TOKEN=
